# ===== Zipkin =====
zipkin:
  enabled: true
  replicaCount: 1
  service:
    type: ClusterIP
    port: 9411
  resources:
    requests:
      memory: "256Mi"
      cpu: "125m"
    limits:
      memory: "1Gi"
      cpu: "500m"
