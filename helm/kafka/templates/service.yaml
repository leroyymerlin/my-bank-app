apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-kafka
  labels:
    app: kafka
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.ports.internal }}
      targetPort: {{ .Values.ports.internal }}
      protocol: TCP
      name: kafka
  selector:
    app: kafka
